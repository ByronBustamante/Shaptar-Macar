<%= form_for(@location_in_warehouse, remote: true) do |f| %>
  <% if @location_in_warehouse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_in_warehouse.errors.count, "error") %> prohibited this location_in_warehouse from being saved:</h2>

      <ul>
      <% @location_in_warehouse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-xs-7 col-md-9 col-lg-10">
      <%= f.text_field :location, :class => 'input-shaptar', :placeholder => 'Ingresar Ubicación' %>
    </div>
    <div class="col-xs-5 col-md-3 col-lg-2">
      <%= f.submit 'Guardar', :class => 'button-shaptar-green' %>
    </div>
  </div>

<% end %>
