<div class="container">

  <div class="row flex-items-xs-center">
    <div class="col-xs-12 col-sm-9 col-md-8 col-lg-5">

      <div class="box-devise">

        <div class="row">
          <div class="col-xs-12">
            <%= image_tag "logo-macar.png", alt: "Macar" %>
          </div>
        </div>

        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div class="row">
          <div class="col-xs-12">
            <%= f.email_field :email, placeholder: "Correo Electrónico", :class => 'input-shaptar', autofocus: true %>
          </div>
          <div class="col-xs-12">
            <%= f.password_field :password, placeholder: "Contraseña", :class => 'input-shaptar' %>
          </div>

        <% if devise_mapping.rememberable? -%>

          <div class="col-xs-12">
            <%= f.check_box :remember_me %>
            <%= f.label :No_cerrar_sesión %>
          </div>
        </div>

        <% end -%>

        <div class="row">
          <div class="col-xs-12">
            <%= f.submit "Iniciar Sesión", :class => 'button-shaptar-white' %>
          </div>
        </div>

        <% end %>

        <%= render "devise/shared/links" %>

      </div>

    </div>
  </div>

</div>
